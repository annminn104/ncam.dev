// Figtree loaded via @fontsource so font files are bundled (works standalone
// and when mounted cross-origin inside the host, unlike <link> in index.html).
import '@fontsource/figtree/400.css';
import '@fontsource/figtree/500.css';
import '@fontsource/figtree/600.css';
